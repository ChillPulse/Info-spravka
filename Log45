Готово по Rec53 — micro-flac backend только для WEB FLAC (SD FLAC остаётся на старом декодере).

1) Вендоринг
Создана папка yoRadio/src/audioI2S/micro_flac/
Скопирован include/micro_flac/flac_decoder.h → micro_flac/micro_flac/flac_decoder.h
Скопированы из src/: alloc.h, bit_reader.h, compiler.h, crc.cpp/.h, decorrelation.cpp/.h, flac_decoder.cpp, frame_header.cpp/.h, lpc.cpp/.h, pcm_packing.cpp/.h, wrapping_arithmetic.h, xtensa/
Лицензия → third_party/micro-flac/LICENSE
2) PSRAM
yoRadio/myoptions.h:

C++

#define MICRO_FLAC_MEMORY_PREFER_PSRAM 1
3) Состояние в AudioEx.h
C++

bool m_mf_active = false;
bool m_mf_header_ready = false;
uint32_t m_mf_sample_rate = 0;
uint8_t m_mf_channels = 0;
uint8_t m_mf_bits = 0;
int32_t* m_mf_out32 = nullptr;
size_t m_mf_out32_cap = 0;
4) Реализация в Audio.cpp
#include "micro_flac/micro_flac/flac_decoder.h"
static micro_flac::FLACDecoder g_mf;
microflac_reset_() — free out32, reset decoder, clear flags; вызывается в setDefaults()
Активация только для WEB: m_dataMode==AUDIO_DATA || ST_WEBFILE && CODEC_FLAC → m_mf_active=true (в processWebStream, processWebFile и lazy в sendBytes)
Декод в sendBytes() для FLAC:
WEB → g_mf.decode(data,len, out32, cap, consumed, samples)
HEADER_READY → setChannels/Rate/Bits, реаллокация out32 по max_block_size
SUCCESS → конвертация int32 left-justified → int16: v>>=(32-bps), затем bps>16 ? >>= : <<=, clamp, m_outBuff[i]
NEED_MORE_DATA, END_OF_STREAM, error
SD → старый FLACDecode() остаётся, используется когда m_mf_active==false
Самопроверка
Bash

git grep -n "micro_flac::FLACDecoder" Audio.cpp # 30: static ...
git grep -n "m_mf_active" AudioEx.h Audio.cpp # 747, 4062, 4174, 4199, 4275, 4314, 5669...
git grep -n "FLAC_DECODER_HEADER_READY" Audio.cpp # 5688
git grep -n "MICRO_FLAC_MEMORY_PREFER_PSRAM" myoptions.h # 19
Критерий успеха
2 тяжёлых FLAC (SECTOR Progressive/Next/Indigo) + 2×128k FLAC — нет бесконечного syncword found, нет циклов старт→стоп без звука, звук стабильный
SD FLAC путь (FLACDecoder_Decode) остался, используется только когда m_mf_active==false
Коммит: 5fe38ad Rec53: micro-flac backend for WEB FLAC radio (SD FLAC unchanged) в main.
